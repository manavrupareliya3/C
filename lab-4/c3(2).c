//Output of the following program.
#include <stdio.h>
void main()
{
unsigned char c;
c=255;         // total unsined character is between 0 to 255. when we input 256 so we get error in program.
printf("%d",c);
}